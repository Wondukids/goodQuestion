# 갈래 후-2 — 리포트 만드는 시점 옮기기 (이슈 #43)

> 이 워크트리는 **혼자 도는 갈래**다. 앞 대화도 프로젝트 메모리도 없다.
> 이 문서 하나로 시작할 수 있게 썼다. **먼저 끝까지 읽어라.**

## ⓪ 여기가 무슨 프로젝트인가

6~9세 아이가 옛이야기(「방귀 뀌는 며느리」) 속 캐릭터와 **말로 대화하는** 앱이다.
활동이 끝나면 아이가 한 말을 모아 **보호자에게 보여 줄 리포트 한 장**을 만든다
(숫자 지표 + LLM 이 쓰는 문장). 그 리포트 기능은 이미 다 서 있고 돌아간다.

이번에 그 앱에 **활동이 하나 더 붙는다** — 이야기를 다 본 뒤의 「말하기 후 활동」
(카드 넉 장 순서 맞추기 → 줄거리 통째로 말하기). 그 결과도 리포트에 실려야 한다.

**그런데 지금은 이야기가 끝나는 순간 리포트가 만들어진다.** 아이가 후활동을 하기도 전에
만들어지니, 후활동 결과가 리포트에 못 들어간다.

## ① 네가 할 일 — 한 줄

**리포트를 「세션이 끝났을 때」가 아니라 「후활동이 끝났을 때」 만들도록 시점을 옮긴다.**

리포트를 처음부터 후활동 결과까지 담아 **한 번에** 만들려는 것이다 (결정 F10). 나중에
숫자만 갈아 끼우는 통로를 만들지 않고, LLM 도 한 세트만 돌린다.

## ② 세 갈래 길 — 이 그림이 네 일의 전부다 (명세 7.1)

```
마지막 장면 끝  →  세션 completed          (지금 그대로. 리포트는 아직 안 만든다)
      │
      ├─ 아이가 후활동을 한다 ─→ 「마치기」  →  complete(reason=finished) ─┐
      ├─ 활동을 건너뛰고 나간다 ───────────→  complete(reason=left)     ─┤
      │                                                                  ├─→ queueReport()
      └─ 앱을 그냥 꺼버린다 ────→ (신호 없음) → 보호자가 리포트를 열 때 ─┘   (F11·F12)
```

**네가 만드는 것은 이 그림의 ①맨 위 줄의 「안 만든다」와 ②맨 아래 줄의 「열 때 만든다」
둘이다.** 가운데 두 줄의 `complete` API 는 **#45 갈래가 만든다** — 네 일이 아니다.

## ③ 시작하기 전에 읽을 것

| 순서 | 파일 | 무엇 |
|---|---|---|
| 1 | `docs/말하기후활동_명세.md` **7.1절** | 🔴 **정본.** 위 그림과 세 갈래 길 |
| 2 | 같은 문서 1절의 **F10~F13** | 왜 이렇게 정했나. 특히 F13 |
| 3 | `docs/보호자_리포트_명세.md` 7·8절 | 지금의 리포트 생성 흐름 (이 문서가 그 절을 **대체한다**) |
| 4 | `src/report/service/generate.ts` | `queueReport()` 와 `generateReport()` 머리말 |
| 5 | `src/report/README.md` | 층 규칙 |

이슈 원문: `gh issue view 43`

## ④ 환경 세팅 — 이것부터

```bash
pnpm install                                                    # node_modules 가 없다
cp "C:/Users/rlaqu/Documents/GitHub/goodQuestion-wondukids/.env.local" .env.local
npx next typegen                       # .next/types 가 없어 typecheck 가 7개 헛터진다
```

### 🔴 검사를 돌릴 때는 `DATABASE_URL` 을 로컬로 바꿔라

본선 `.env.local` 은 **팀의 라이브 Supabase** 를 가리킨다. 진짜 아이 계정이 있고, 레포
가드가 로컬 아닌 주소면 DB 검사를 **막는다** — 그대로 두면 DB 검사가 통째로 안 돈다.

```
DATABASE_URL="postgresql://postgres:gqlocal@127.0.0.1:5432/goodquestion_ts"
```

로컬 도커 `gq-pg`(postgres:16)에 **007 까지 먹여져 있다.** 후활동 표도 이미 선다.
⛔ `drizzle-kit push` 는 어떤 이유로도 치지 마라. ⛔ 팀 Supabase 에 쓰지 마라.

## ⑤ 만들 것 (1) — 「후활동이 있는 이야기면 여기서 안 만든다」

지금 리포트를 부르는 자리가 **둘**이다. `#38` 머지로 이미 `api_team` 에 들어와 있다.

| 어디 | 언제 부르나 |
|---|---|
| `src/llm/service/run.ts:318` | 회차를 닫는 자리 |
| `src/llm/service/story.ts:228` | 이야기 세션이 끝나는 자리 |

🔴 **지우지 마라. 조건을 걸어라.** 그 이야기에 `post_activity_config` 가 **없을 때만**
부른다. 지우면 **후활동이 없는 이야기의 리포트가 영영 안 만들어진다** (그 이야기는 끝
화면에 활동 버튼이 없어 `complete` 신호가 안 온다 — 결정 F13 이 그 길이다).

## ⑥ 만들 것 (2) — 받침: 보호자가 열었는데 없으면 그 자리에서 만든다 (F12)

아이가 활동을 안 하고 **앱을 그냥 꺼버리면** `complete` 신호가 영영 안 온다. 그때를
받치는 자리다. 리포트를 읽는 길은 `src/report/controller/` 에 있다 (`report.ts`·`list.ts`).

- 늦게 만들어도 손해 보는 사람이 없다. 그 사이 아이가 순서만 맞추고 껐다면 **그것까지**
  담긴다.
- ⚠️ **여기가 이 갈래에서 가장 판단이 필요한 자리다.** 리포트 한 장에 **LLM 이 2회** 나가
  보호자의 첫 열람이 느려진다. 명세 11절이 「리포트 화면에 **「만드는 중」 상태**가 필요하다」
  고 이미 적어 뒀고, 리포트 화면(#39)은 이미 머지돼 있다.
  **동기로 만들어 기다리게 할지, 만들기 시작만 하고 「만드는 중」을 돌려줄지 네가 정해라.**
  🔴 **되돌리기 쉬운 쪽을 고르고, 무엇을 왜 골랐는지 커밋 본문과 마지막 답변에 적어라.**
  화면 쪽 파일을 고쳐야 한다면 **고치지 말고 무엇이 필요한지 적어라** (다른 갈래 것이다).

## ⑦ 🔴 놓치면 사고 나는 것

### ① 세션 상태를 건드리지 마라

마지막 장면이 끝나면 **지금처럼 곧바로 `completed`** 다 (`src/llm/repo/sessions.ts:566`).
한때 `post_activity` 라는 상태를 쓰려다 **접었다**(F5) — 활동을 안 하고 앱을 꺼버린 아이의
세션이 그 상태에 영영 갇히고 리포트가 안 나오기 때문이다. 그 상태 값은 체크 제약과 화면
문구에 남아 있지만 **쓰이지 않는 값**이다. 되살리지 마라.

### ② `queueReport()` 의 「이미 있으면 돌아간다」를 깨지 마라

세 갈래 길 중 둘이 겹쳐 들어와도 두 번 만들지 않는 것이 그 규칙이다. 아이가 마치고 나서
끝 화면을 떠나면 `complete` 가 **두 번** 불리는데, 두 번째는 아무 일도 안 일어나야 한다.

### ③ `queueReport()` 는 「아직 안 끝난 활동」을 거른다

지금 `신원.status !== 'completed'` 면 그냥 돌아간다. 후활동은 **이미 `completed` 인 세션**에
붙는 활동이라 이 문지기와 어긋나지 않는다 — 확인만 하고 넘어가라.

### ④ 후활동 결과를 리포트 **숫자**에 싣는 것은 네 일이 아니다

`metrics.post_activity` 를 채우는 것은 **#47 갈래**다 (명세 7.2). 네 갈래는 **언제 만드나**만
옮긴다. 지표 모양(`src/report/types.ts`)을 고치지 마라 — 여러 갈래가 공유하는 계약이다.

## ⑧ 손대지 마라

- ⛔ **`src/report/types.ts`** — 공유 계약. 모자라면 고치지 말고 마지막 답변에 적어라.
- ⛔ **`src/report/domain/metrics.ts` · `src/report/engine/**`** — 앞 갈래가 끝낸 것이다.
- ⛔ **화면 파일** (`src/lib/report.ts` · `src/app/report/**` · `src/components/report/**`).
- ⛔ **후활동 API 넷** (명세 5절) — #45 것이다. `complete` 라우트를 만들지 마라.
- ⛔ **판정 엔진·프롬프트** (`prompts/retelling_keywords/`) — **지금 다른 워크트리가 그
   갈래를 짜고 있다.** 열어 보더라도 읽기만 해라.
- ⛔ **미션 1·2** (`mission_*`) · `sql/**` · `src/llm/db/schema.ts` — 표는 다 서 있다.

## ⑨ 다 하면

- [ ] 명세 9절의 **수용 기준 12·13·14** 를 직접 확인한다:
      12. 활동을 안 하고 끝 화면을 떠나도 리포트가 만들어진다
      13. 아무 신호 없이 앱을 꺼도, 보호자가 리포트를 열면 그 자리에서 만들어진다
      14. **이야기가 끝난 것만으로는 리포트가 만들어지지 않는다** (후활동이 있는 이야기에서)
      ⚠️ 14 와 F13(후활동 없는 이야기는 지금처럼)이 **둘 다** 서야 한다. 검사로 남겨라.
- [ ] `pnpm typecheck` 통과 · `pnpm lint` 는 변경 전과 같은지만 본다.
- [ ] `pnpm test` — **기준선 대비 실패가 늘지 않았다.** 🔴 시작하자마자 아무것도 고치기
      전에 한 번 돌려 기준선을 재 둬라. DB 검사가 5초 제한에서 흔들려 회차마다 숫자가
      크게 다르다(같은 트리에서 2·5·12·25 실패가 나온 적이 있다). **짝지어 견줘라.**

```bash
git add -A
git commit    # 첫 줄은 한국어 한 문장, 끝에 ` — #43`
git push -u origin rlaqudwn1/issue-43-report-timing
```

본문에 「무엇을 왜」와 「확인한 것」. `git log -3` 을 본보기로 봐라. 끝에 트레일러:

```
Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
```

⛔ **PR 을 열지 마라. 머지하지 마라.** 푸시까지만 하고 마지막 답변에 상황을 적어라.

## ⑩ 막히면

- 판단이 갈리면 **명세를 이긴다고 생각하지 말고** 명세대로 하고, 이상한 점을 적어라.
- 명세에 없는 것을 정해야 하면 **가장 되돌리기 쉬운 쪽**을 고르고 그 사실을 적어라.
- 엔진 쪽 코드(`src/llm` · `src/report` · `src/session`)는 **한국어 식별자와 한국어 주석**을
  쓴다. 주변 코드의 결을 따라라.
