# 갈래 P-6 — 리포트 목데이터 아이 10명 (이슈 #40)

> 이 워크트리는 **혼자 도는 병렬 갈래**다. 앞 대화도 프로젝트 메모리도 없다.
> 이 문서 하나로 시작할 수 있게 썼다. **먼저 끝까지 읽어라.**

## ⓪ 여기가 무슨 프로젝트인가

6~9세 아이가 옛이야기(「방귀 뀌는 며느리」) 속 캐릭터와 **말로 대화하는** 앱이다.
아이가 한 말을 LLM 이 읽고 「감정을 말했나 · 까닭을 붙였나」 같은 **사고 요소 8종**을
찾아 DB 에 기록한다. 활동이 끝나면 그 기록을 모아 **보호자에게 보여 줄 리포트 한 장**을
만든다. 이 갈래는 그 리포트를 **여러 판으로 눈으로 보기 위한 아이 10명**을 심는 일이다.

## ① 네가 할 일 — 한 줄

**아이 10명의 발화 대본을 손으로 심고, 그 뒤는 진짜 코드가 돌려 만들게 한다** (결정 R13).

즉 시드가 넣는 것은 **아이 행 · 세션 행 · 발화 대본뿐**이다. 발화분석 · 지표 집계 ·
리포트 생성은 **실제 파이프라인**이 돈다. 그래야 배선이 진짜로 도는지가 같이 확인된다.

## ② 시작하기 전에 읽을 것

| 순서 | 파일 | 무엇 |
|---|---|---|
| 1 | `docs/보호자_리포트_명세.md` **10절** | 🔴 **정본.** 아이 10명의 표 |
| 2 | `src/llm/db/seed.ts` | 콘텐츠 시드. **본보기이자 섞지 말아야 할 파일** |
| 3 | `src/report/domain/metrics.ts` 의 `집계재료` | 네 대본이 결국 무엇으로 읽히는지 |
| 4 | `src/llm/config.ts` (제미나이 키 부분) · `src/llm/provider/index.ts:311~365` | 키 셋을 어떻게 고르나 |
| 5 | `src/llm/service/goldenset.ts` | **여러 건을 키 지정해 돌리는 본보기** |

이슈 원문: `gh issue view 40`

## ③ 심을 아이 10명 (명세 10절)

이야기는 **「방귀 뀌는 며느리」 하나**. 나이 6~9세.

| 아이 | 나이 | 활동 | 무엇을 보려고 |
|---|---|---|---|
| 1~4 | 6·7·8·9 | 완주 1건 | 표준 그림. **나이별 문장 길이 차이** |
| 5 | 7 | 완주 1건 | 감정 낱말이 많은 아이 — 「감정 표현」 축이 튀는 그림 |
| 6 | 6 | 완주 1건 | 짧게만 답한 아이 — 보완할 점이 많고 유도가 잦다 |
| 7 | 8 | 완주 1건 | 짧게 답했다가 **되묻기에 회복** — 상호작용 지표 확인 |
| 8 | 9 | **완주 3건** | 드롭다운 · 새 낱말 누적 · 재방문 |
| 9 | 7 | 완주 1건 (이전 0건) | **첫 활동** — `child_words` 가 빈 상태 |
| 10 | 8 | **중단 1건** (발화 3건) | 빈약한 활동의 빈 칸 처리 |

자리: **`src/llm/db/seed-report-mock.ts`** (콘텐츠 시드 `seed.ts` 와 섞지 않는다).

### 🔴 진짜 계정과 섞이면 안 된다

`children` 표에는 **진짜 아이 계정 14행**이 있다. 이름 앞에 표식을 두고(기존
`[실험용]` 접두사 같은 결), **지우는 길을 같은 스크립트에** 만들어라. 지우기를 돌리면
목데이터만 사라지고 진짜 계정은 그대로여야 한다.

### 대본을 쓸 때

- 발화는 **6~9세가 실제로 할 법한 한국어**여야 한다. 나이가 어릴수록 짧고, 9세는
  「왜냐하면」으로 이유를 잇는다.
- 대사가 **장면의 요구 요소와 맞물려야** 분석 LLM 이 요소를 잡는다. 장면 정의는
  `src/llm/db/seed.ts` 의 대화 장면 넷(`sc_banggui_03·05·07·09`)에 있다.
- 아이 7번의 「되묻기 회복」은 **유도(GUIDED) 턴 다음 발화가 `VALID`** 여야 지표에 잡힌다.
- 아이 8번의 3건은 **뒤 활동일수록 새 낱말이 적어야** 한다 — 같은 낱말을 다시 쓰게 짜라.

## ④ 🔴 키 셋 병렬 — **프로세스를 셋 띄운다** (공유 파일을 고치지 않는다)

분석이 발화 한 건당 LLM 1회라 200회가 넘는다. 그래서 **API 키 셋으로 나눠 돌린다.**
분당 한도는 **공급자 쪽에서 키마다 따로** 걸리므로, 키를 갈라 쓰면 예산이 셋이 된다.

### 알고 있어야 할 것 — 한 프로세스 안에서는 갈라지지 않는다

`src/llm/provider/rate-limit.ts` 의 `waitForRateLimit(rpm, label)` 은
**모듈 전역 배열 `recentCalls` 하나**로 미끄럼창을 잰다. **키별이 아니다.**
그래서 **한 프로세스 안에서** 키 1·2·3 으로 세 갈래를 돌리면 셋이 **같은 예산 하나**를
나눠 쓴다 — 일꾼만 셋이고 처리량은 그대로다.

### 그래서 프로세스를 셋 띄운다

```bash
# 세 창(또는 & 로 백그라운드)에서 나란히
tsx src/llm/db/seed-report-mock.ts --analyze --key 1 --shard 0/3
tsx src/llm/db/seed-report-mock.ts --analyze --key 2 --shard 1/3
tsx src/llm/db/seed-report-mock.ts --analyze --key 3 --shard 2/3
```

**프로세스가 다르면 모듈 전역도 다르다.** `recentCalls` 가 셋으로 갈라져 창 공유 문제가
**애초에 생기지 않는다.** ⭐ **그래서 `rate-limit.ts` 도 `provider/index.ts` 도 고치지 마라** —
둘 다 다른 갈래가 쓰는 공유 파일이라 안 건드리는 것이 훨씬 싸다.

**스크립트가 받아야 할 것 둘:**

- `--key <1|2|3>` — 그 프로세스가 쓸 제미나이 키. `preferred_gemini_key` 로 넘긴다
  (`src/llm/provider/index.ts:311`). 검사는 `제미나이_키_확인()`
  (`src/llm/service/goldenset.ts:526`) 이 이미 있다 — **한 건이라도 부르기 전에** 막는다.
- `--shard <i>/<n>` — 일감을 겹치지 않게 가른다. **활동(세션) 단위로 갈라라** — 발화
  단위로 가르면 한 활동이 세 프로세스에 흩어져 나중에 뭐가 빠졌는지 보기 어렵다.
  가르는 기준은 **정해진 순서**여야 한다(활동 목록을 정렬한 뒤 `index % n === i`).
  난수로 가르면 다시 돌릴 때 다른 것이 걸린다.

`src/llm/service/goldenset.ts` 가 「키를 지정해 여러 건을 돌리는」 본보기다.

⚠️ **연속 실패에 멈추는 장치를 넣어라.** goldenset 이 「연속 3건 실패하면 멈춘다」를
쓰는 이유가 있다 — 키가 통째로 막힌 상태에서 남은 건을 계속 던지면 시간만 태운다.

⚠️ **세 프로세스가 같은 DB 에 동시에 쓴다.** 같은 행을 두 프로세스가 건드리지 않게
샤드로 갈라 두면 충돌할 일이 없지만, **upsert 로 쓰고** 넣기 전에 「이미 있나」를 봐라.

## ⑤ 💰 비용 — 옵션으로 가른다

발화 한 건당 분석 LLM **1회**다. 10명 12건 활동에 **대략 200~250회**.
리포트 생성은 활동 하나당 **2회** 더.

**「어디까지 돌릴지」를 옵션으로 둬라.** 최소 세 단계:

| 단계 | 무엇까지 | 비용 |
|---|---|---|
| `--seed-only` | 아이·세션·발화 대본만 | 0회 |
| `--analyze` | + 발화분석 | 200~250회 |
| `--report` | + 리포트 생성 | + 활동당 2회 |

대본만 넣고 멈출 수 있어야 **다시 돌릴 때 앞 단계를 안 태운다.** 반복 안전(upsert)해야
한다 — 두 번 돌려도 아이가 20명이 되면 안 된다.

## ⑥ 환경 — 어느 DB 에 심나

```bash
pnpm install
cp "C:/Users/rlaqu/Documents/GitHub/goodQuestion-wondukids/.env.local" .env.local
```

### 🔴 검사는 로컬, 실제 목데이터는 팀 Supabase

- **검사(`vitest`)를 돌릴 때는 로컬로 바꿔라.**
  `DATABASE_URL="postgresql://postgres:gqlocal@127.0.0.1:5432/goodquestion_ts"`
  (도커 `gq-pg` 가 떠 있다. 006 을 먹여야 표가 생긴다:
  `docker cp sql/006_parent_report.sql gq-pg:/tmp/006.sql && docker exec gq-pg psql "postgresql://postgres:gqlocal@127.0.0.1:5432/goodquestion_ts" -f /tmp/006.sql`)
- **진짜 목데이터를 심는 것은 팀 Supabase 다** (본선 `.env.local` 의 원래 주소).
  화면이 읽는 곳이 거기라서다. 표 셋과 낱말 시드는 **본선이 이미 적용해 뒀다.**

⛔ **`drizzle-kit push` 는 어떤 이유로도 치지 마라.** 선언에 없는 표를 지운다.
⛔ 팀 Supabase 를 가리킨 채 `vitest` 를 돌리지 마라.
⛔ `children` 의 진짜 14행, `messages`·`story_sessions` 의 기존 행을 **고치거나 지우지 마라.**
   너는 **새 행만 넣는다.**

## ⑦ ⏳ 선행이 하나 남았다 — #38

리포트를 실제로 **만드는** 코드(`generateReport()` · API 넷)가 **#38 갈래에서 지금 짜이는
중**이다. 아직 `api_team` 에 없다.

**그래서 이 순서로 해라 — 기다리지 말고 앞부터 한다.**

1. **먼저** 대본과 시드 스크립트(`--seed-only`)를 완성한다. #38 이 없어도 다 된다.
2. **그다음** 분석까지(`--analyze`) — 이것도 #38 없이 된다
   (`src/llm/engine/analyze.ts` 가 이미 있다).
3. 키 셋 병렬(④)도 여기까지에서 다 확인된다.
4. **리포트 생성(`--report`)은 #38 이 합쳐진 뒤에** 잇는다. 그 전에는 부를 함수가 없다 —
   `generateReport()` 를 **네가 만들지 마라.** #38 것이다. 자리만 비워 두고 보고해라.

## ⑧ 끝났다고 말할 수 있는 조건

- [ ] 10명 전부 심긴다 — 중단 활동과 첫 활동 포함.
- [ ] 아이 8번은 활동이 **3건**이고, 두 번째·세 번째의 「새로 쓴 낱말」이 첫 번째보다 **적다.**
- [ ] 아이 9번의 `prior_activities` 가 **0** 이다.
- [ ] 아이 10번은 발화 3건이고 `axes` 가 대부분 0 인데도 **리포트가 만들어진다.**
- [ ] 지우기를 돌리면 **목데이터만** 사라지고 진짜 계정 14행은 그대로다.
- [ ] 두 번 돌려도 아이가 20명이 되지 않는다 (반복 안전).
- [ ] 키 셋으로 **프로세스 셋을 나란히 돌려 실제로 빨라진 것을 숫자로 보였다** (④).
      샤드가 겹치지 않아 **같은 발화를 두 번 분석하지 않았다.**
- [ ] `src/llm/provider/rate-limit.ts` 와 `provider/index.ts` 를 **안 고쳤다** (④).
- [ ] `pnpm typecheck` · `pnpm lint` 통과.
- [ ] `pnpm test` — **기준선 대비 실패가 늘지 않았다.** 🔴 **시작하자마자 기준선을 재 둬라.**

마지막 조건 하나 더 — 이 결과로 **#36 의 상호작용 점수 공식(M1)을 조정할지 판단**한다.
그 공식은 실측 없이 정한 임의값이라 목데이터로 처음 재 보는 것이다.
**고치지는 말고, 나온 숫자와 네 판단을 마지막 답변에 적어라.**

## ⑨ 남의 갈래를 건드리지 마라

**#38(API·배선)과 #39(화면 배선)가 다른 워크트리에서 돌고 있다.**

| 갈래 | 만지는 곳 |
|---|---|
| **P-6 (너, #40)** | `src/llm/db/seed-report-mock.ts` · (필요시) `src/llm/provider/rate-limit.ts`·`provider/index.ts` · 관련 검사 |
| P-4 (#38) | `src/report/controller/` · `service/` · `repo/` · `src/app/api/**` |
| P-5 (#39) | `src/lib/report.ts` · `src/app/report/page.tsx` · `src/components/report/**` |

- ⛔ `src/report/**` 에 **손대지 마라.** 앞 갈래들 것이다.
- ⛔ `src/llm/db/seed.ts` 를 고치지 마라. **콘텐츠 시드다. 새 파일에 쓴다.**
- ⛔ `src/report/types.ts` 를 고치지 마라. 모자라면 보고해라.
- ⚠️ `rate-limit.ts` · `provider/index.ts` 는 **공유 파일**이다. ④의 고침만 최소한으로
  하고, 다른 것은 건드리지 마라.

## ⑩ 다 하면

```bash
git add -A
git commit    # 첫 줄은 한국어 한 문장, 끝에 ` — #40`
git push -u origin rlaqudwn1/issue-40-report-mock
```

트레일러 두 줄:

```
Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01FKq5C6XMcXiiNAqikbRdfi
```

⛔ **PR 을 열지 마라. 머지하지 마라.** 푸시까지만 하고 마지막 답변에 상황을 적어라.

## ⑪ 막히면

- 판단이 갈리면 **명세를 이긴다고 생각하지 말고** 명세대로 하고, 이상한 점을 적어라.
- 명세에 없는 것을 정해야 하면 **가장 되돌리기 쉬운 쪽**을 고르고 그 사실을 적어라.
- 이 레포의 엔진 쪽 코드는 **한국어 식별자와 한국어 주석**을 쓴다. 주변 결을 따라라.
