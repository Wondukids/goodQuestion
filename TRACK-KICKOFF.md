# 갈래 후-3ᵃ — 단어 판정 프롬프트 (이슈 #44 의 앞쪽 절반)

> 이 워크트리는 **혼자 도는 갈래**다. 앞 대화도 프로젝트 메모리도 없다.
> 이 문서 하나로 시작할 수 있게 썼다. **먼저 끝까지 읽어라.**

## ⓪ 여기가 무슨 프로젝트인가

6~9세 아이가 옛이야기(「방귀 뀌는 며느리」) 속 캐릭터와 **말로 대화하는** 앱이다.
아이가 이야기를 다 보고 나면 활동이 하나 더 있다 — **말하기 후 활동**. 장면 카드 넉 장을
이야기 순서대로 놓고, 그 카드와 「핵심 단어 12개」를 보면서 줄거리를 통째로 들려주는
활동이다.

아이가 말한 줄거리를 서버가 받아쓴 다음, **핵심 단어 12개를 아이가 썼는지**를 가린다.
판정은 세 단이다:

```
① 규칙   글자로 그대로 찾힌다      → used     (LLM 안 부른다)
② LLM    「비슷한 말」로 말했나     → similar  ← 🔴 네가 쓰는 프롬프트가 이 자리다
③ 대조   근거가 아이 원문에 없으면  → missing  (지어낸 판정을 버린다)
```

**네가 쓰는 것은 ②에게 주는 지시문 한 벌**이다. 한국어는 「참다」를 「참았어요」·「꾹
눌렀어요」로 말하기 때문에 글자 대조만으로는 못 잡는다. 그 가운데 층을 LLM 이 맡는다.

## ① 네가 할 일 — 한 줄

**`prompts/retelling_keywords/` 폴더를 새로 세운다. 파일 둘, 그것뿐이다.**

⛔ **판정 엔진 코드(TypeScript)는 짜지 마라.** 그건 표(#42)가 선 뒤에 다른 워크트리가
한다. 네 갈래는 **DB 를 하나도 안 쓰기 때문에** 지금 나란히 도는 것이다.

## ② 🟢 이미 정해진 것 — 새로 판단하지 마라

| 이미 있는 것 | 어디 |
|---|---|
| 판정 규칙·재료·출력 규격 | `docs/말하기후활동_명세.md` **6절** (6.1 · 6.2 · 6.3) |
| **단어 12개의 통과·탈락 예시 표** | 같은 문서 **6.4절** — 🔴 **사람 확인까지 끝난 표다** |
| 프롬프트 폴더의 규율 | `prompts/README.md` — 🔴 **읽지 않고 쓰면 반드시 검사가 빨개진다** |
| 가장 가까운 본보기 | `prompts/judge_gave_away_element/` (한국어 · 판정 · 근거 인용) |

🔴 **6.4 표를 다시 판단하지 마라.** 「비슷한 말」의 경계는 그 표에서만 정하기로 확정됐다
(인계 문서 6절). 특히 이 한 줄은 **사람이 직접 가른 것**이라 그대로 옮겨야 한다:

> **「걱정」에 「무서웠어요」는 탈락이다.** 걱정은 앞일을 염려하는 것이고 무서움은 지금
> 겁나는 것이라 감정이 다르다. **탈락 예시로 프롬프트에 명시한다.**

## ③ 시작하기 전에 읽을 것

| 순서 | 파일 | 무엇 |
|---|---|---|
| 1 | `docs/말하기후활동_명세.md` **6절** | 🔴 정본. 재료·출력·지켜야 할 것·12줄 예시 표 |
| 2 | `prompts/README.md` | 🔴 **1·2·3·4절이 곧 네 채점표다** |
| 3 | `prompts/judge_gave_away_element/` 두 파일 | 한국어 판정 프롬프트의 결 |
| 4 | `tests/prompts.test.ts` | 네가 고쳐야 할 검사 (⑥) |

이슈 원문: `gh issue view 44` (그중 **프롬프트 두 파일까지만** 네 몫이다)

## ④ 환경 세팅

```bash
pnpm install                                                    # node_modules 가 없다
cp "C:/Users/rlaqu/Documents/GitHub/goodQuestion-wondukids/.env.local" .env.local
```

DB 는 안 쓴다. `.env.local` 은 검사가 부팅될 때 필요해서 복사할 뿐이다.
`npx vitest run tests/prompts.test.ts` 가 네 주된 신호다.

## ⑤ 만들 것 — 파일 둘

### `prompts/retelling_keywords/보낼것.md` — **이 파일이 곧 LLM 에 나가는 글자다**

오리거나 다듬는 자리가 없다. 여기 쓴 글자는 전부 모델이 읽는다.

담을 것 (명세 6.1·6.2):

- **역할 한 문단** — 아이(6~9세)가 방금 들려준 옛이야기 줄거리를 읽고, 주어진 단어를
  「비슷한 말로 말했는가」만 가린다.
- **판정 하나만 한다** — 이 프롬프트는 `similar` 여부만 낸다. `used`(글자로 찾힘)는
  부르기 전에 규칙이 이미 걸러 냈고, 여기 오는 단어는 **못 찾은 것뿐**이다.
- **지켜야 할 것 넷** (명세 6.2 — 규칙마다 태그를 붙인다):
  - 아이 말을 고쳐 쓰지 않는다. `evidence` 는 **원문에서 떼어 온 조각 그대로**다.
  - 없는 말을 만들지 않는다. **애매하면 `similar: false`** — 의심스러우면 안 쓴 것으로 본다.
  - 점수·칭찬·조언을 쓰지 않는다. 그건 보호자 리포트 몫이다.
  - 줄거리를 판단하지 않는다. 이번 판정은 **단어 목록**에 대한 것뿐이다.
- **단어별 통과·탈락 예시 12줄** — 명세 6.4 표 그대로.
- **출력 규격**:

```json
{ "words": [ { "word": "참다", "similar": true, "evidence": "방귀를 꾹 눌렀어요" } ] }
```

- 🟢 **지어낸 근거는 부르는 쪽이 버린다**는 사실을 적어 둬라 — 원문에 없는 `evidence` 를
  내면 그 단어는 `missing` 으로 떨어진다. 모델에게 지어낼 이유가 없다는 것을 알려 준다.

### `prompts/retelling_keywords/해설.md` — **한 글자도 LLM 에 안 간다**

왜 이렇게 정했나 · 출처 · 재료 열쇠 표 · 검수 이력. `보낼것.md` 와 **같은 태그**를 쓴다
(⑥①이 그 짝을 검사한다). 「무서웠어요」를 가른 판단과 그 날짜(2026-08-15 사람 확정)를
여기 남겨라.

### 🔴 언어는 **한국어**다

판정 대상이 한국어 발화이고, 근거로 **아이 말에서 글자를 그대로 잘라 내야** 한다.
심판 셋(`judge_*`)이 한국어인 것과 같은 이유다 (`prompts/README.md` 4절). 분석·캐릭터가
영어인 것은 토큰을 아끼려는 것이고, 여기서는 그 이유가 판정 정확도에 진다.

## ⑥ 🔴 함께 고칠 것 — 안 고치면 검사가 빨개진다

`prompts/README.md` 를 안 읽고 쓰면 여기서 걸린다. 미리 적어 둔다.

1. **`tests/prompts.test.ts` 의 `재료를_받는_것` 목록**(45행)에 `'retelling_keywords'`
   를 더한다. 폴더 목록 자체는 디스크에서 읽으므로(67행) 목록에 안 넣으면
   「정본 열이 다 읽힌다」가 곧바로 깨진다.
2. **태그 80쌍 고정 단언**(132행). 네 프롬프트의 태그 수만큼 늘려 적고, 주석의 셈식에도
   한 줄을 더해라.
3. **태그 머리글자가 겹치면 안 된다.** 이미 쓰는 것: `E`(분석) · `C`(캐릭터) · `K`(아이) ·
   `R`(미션대답) · `S`(미션요약) · `A`(리포트분석) · `G`(리포트가이드).
   **`[W-…]`(Word) 를 권한다.** 겹치는지 직접 확인하고 골라라.
4. **`보낼것.md` 에 인용을 남기지 마라** — `docs/` · `CLAUDE.md` · `notes/` · `이슈 #` ·
   📄 · ✏️ 가 한 글자라도 있으면 「인용 누출」 검사가 잡는다. 근거는 전부 `해설.md` 로.
5. **자리표시자 금지** — `{word}` · `{child_text}` 같은 것을 쓰지 마라. **안 채워지고 글자
   그대로 모델에게 나간다.** user 본문은 언제나 코드가 만드는 재료 JSON 한 덩이다.
   (괄호 안이 이름 하나뿐인 글자를 검사가 잡는다.)
6. **`prompts/README.md` 를 갱신한다** — 0절 폴더 목록(열 → 열하나) · 3절 태그 표 ·
   4절 언어 표 · 8절 「열 프롬프트」 표에 새 줄. 낡은 README 가 다음 사람을 속인다.

## ⑦ 🔴 뒤 갈래와의 약속 — 여기서 어긋나면 나중에 못 찾는다

너 다음에 **판정 엔진**을 짜는 갈래가 이 프롬프트를 부른다. 둘 사이의 경계를 못박아 둔다.

- **12줄 예시 표는 `보낼것.md` 안에 산다.** 코드에 두지 않는다 (명세 6.4 · 인계 6절 확정).
  판정이 흔들리면 표를 고치지 엔진을 고치지 않는다.
- **재료(user JSON)에는 「판정할 단어 목록」만 들어간다** — 규칙이 이미 거르고 남은 것.
  이야기 제목과 한 줄 요지, 카드 넉 장의 제목(아이가 본 순서), 아이가 말한 줄거리 원문이
  함께 간다 (명세 6.1). 그 JSON 을 만드는 것은 **엔진 갈래의 일**이다.
- **폴더 이름은 `retelling_keywords` 다.** 엔진이 이 이름으로 파일을 읽는다. 바꾸지 마라.
- 활동 한 건에 **LLM 호출 한 번**이다 (명세 6.3). 단어마다 부르는 모양으로 쓰지 마라.

## ⑧ 손대지 마라

- ⛔ **`src/**` 의 어떤 파일도.** 이번 갈래는 md 두 개 + 검사 하나 + README 다.
- ⛔ **다른 프롬프트 폴더 아홉.** 특히 `analysis/보낼것.md` — 그 파일의 sha256 이 골든셋
   회차에 박제돼 있어 한 글자만 바뀌어도 이전 판과의 비교가 끊긴다.
- ⛔ **`prompts/README.md` 의 규율 자체를 고치지 마라.** 목록·표에 네 줄을 더하는 것까지다.
- ⛔ **실험판(`/prompt-lab`) 배선에 얹으려 하지 마라.** 실험 대상은 `analysis`·`character`
   둘뿐이고, 세 곳(리포·DB CHECK·화면)에 못박혀 있다 (README 6절 ②).
- ⛔ 미션 1·2 (`mission_*`) 와 리포트 프롬프트 둘(`report_*`)에 손대지 않는다.
- 🟡 **`sql/` · `src/llm/db/` 는 지금 다른 워크트리(#42)가 쓰고 있다.** 네 갈래와 겹칠 일이
   없지만, 혹시 열어 보게 되면 읽기만 해라.

## ⑨ 다 하면

- [ ] `npx vitest run tests/prompts.test.ts` **전부 통과** — 태그 짝 · 인용 누출 ·
      자리표시자 · 폴더 목록.
- [ ] `pnpm typecheck` 통과.
- [ ] `pnpm test` — **기준선 대비 실패가 늘지 않았다.** 🔴 시작하자마자 아무것도 고치기 전에
      한 번 돌려 기준선을 재 둬라 (DB 검사가 시간제한에서 흔들려 숫자가 매번 다르다).
- [ ] 두 파일을 **나란히 놓고 사람처럼 한 번 읽어라.** 검사는 「태그가 양쪽에 있느냐」만
      보고 **뜻이 어긋난 것은 못 잡는다** (검사 자신이 그렇게 적어 뒀다).

```bash
git add -A
git commit    # 아래 형식
git push -u origin rlaqudwn1/issue-44-retelling-prompt
```

커밋 메시지는 이 레포의 결을 따른다 — **첫 줄은 한국어 한 문장, 끝에 ` — #44`**.
본문에 「무엇을 왜」와 「확인한 것」. `git log -3` 을 본보기로 봐라. 끝에 트레일러:

```
Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
```

⛔ **PR 을 열지 마라. 머지하지 마라.** 푸시까지만 하고 마지막 답변에 상황을 적어라.

**마지막 답변에 꼭 적을 것** — 태그를 몇 개 썼는지(합계를 몇으로 고쳤는지)와, 6.4 표에서
**옮기다 애매했던 줄이 있었다면 어느 줄인지.** 그 표는 사람이 확정한 것이라, 네가 흔들린
자리는 사람이 다시 봐야 한다.

## ⑩ 막히면

- 판단이 갈리면 **명세를 이긴다고 생각하지 말고** 명세대로 하고, 이상한 점을 적어라.
- 명세에 없는 것을 정해야 하면 **가장 되돌리기 쉬운 쪽**을 고르고 그 사실을 적어라.
- 🔴 **6.4 표의 통과·탈락 판단을 네 판단으로 바꾸지 마라.** 사람이 확정한 경계다.
  더 좋은 예시가 떠오르면 **더하지 말고 마지막 답변에 적어라.**
