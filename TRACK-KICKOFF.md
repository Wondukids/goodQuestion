# 갈래 P-3 — 리포트 프롬프트 2편·엔진 (이슈 #37)

> 이 워크트리는 **혼자 도는 병렬 갈래**다. 앞 대화도 프로젝트 메모리도 없다.
> 이 문서 하나로 시작할 수 있게 썼다. **먼저 끝까지 읽어라.**

## ⓪ 여기가 무슨 프로젝트인가

6~9세 아이가 옛이야기(「방귀 뀌는 며느리」) 속 캐릭터와 **말로 대화하는** 앱이다.
아이가 한 말을 LLM 이 읽고 「감정을 말했나 · 까닭을 붙였나」 같은 **사고 요소 8종**을
찾아 DB 에 기록한다. 지금 만들려는 것은 그 기록을 모아 **활동이 끝난 뒤 보호자에게 보여 줄
리포트 한 장**이고, 그 리포트의 **문장을 쓰는 부분**이 이 갈래다.

숫자(몇 번 나왔나)는 규칙이 세고 **문장은 LLM 이 쓴다** (결정 R3). 네가 만드는 것은 그
문장 쪽이다 — 총평 · 역량 3카드 · 대표 발화 고르기 · 어휘 팁 · 가정 연계 질문 6개.

## ① 네가 할 일 — 한 줄

**프롬프트 폴더 둘을 쓰고, 그것을 부른 뒤 응답을 원문과 대조해 걸러 내는 엔진을 짠다.**

## ② 시작하기 전에 읽을 것 (전부 이 워크트리에 있다)

| 순서 | 파일 | 무엇 |
|---|---|---|
| 1 | `prompts/README.md` | 🔴 **읽지 않고 프롬프트를 쓰지 마라.** 두 파일 체제 · 태그 규약 · 자리표시자 금지 |
| 2 | `docs/보호자_리포트_명세.md` **5절** | 🔴 **정본.** 5.2 · 5.4 의 출력 JSON 규격 · 5.5 지켜야 할 것 |
| 3 | `src/report/types.ts` | 네가 만들 `ReportNarrative` 의 **선언**. 여기 있는 그대로 채운다 |
| 4 | `prompts/analysis/보낼것.md` · `해설.md` | 본보기 한 쌍. 태그가 어떻게 붙는지 눈으로 봐라 |
| 5 | `src/llm/engine/analyze.ts:242` | LLM 을 부르는 자리의 본보기 (`chooseBody` → `complete`) |
| 6 | `src/llm/judge.ts:420~435` | **근거를 원문과 대조해 어긋나면 버리는** 방식의 본보기 |

이슈 원문도 볼 수 있다: `gh issue view 37`

## ③ 환경 세팅 — 이것부터

이 워크트리에는 **git 이 추적하지 않는 파일이 없다.**

```bash
pnpm install                                                    # node_modules 가 없다
cp "C:/Users/rlaqu/Documents/GitHub/goodQuestion-wondukids/.env.local" .env.local
```

### 🔴 복사한 뒤 `DATABASE_URL` 을 반드시 로컬로 바꿔라

본선 `.env.local` 의 `DATABASE_URL` 은 **팀 레포의 라이브 Supabase** 를 가리킨다.
그 DB 에는 **진짜 아이 계정 8행**이 들어 있고, `vitest` 는 진짜 DB 에 행을 넣고 지운다.

```
DATABASE_URL="postgresql://postgres:gqlocal@127.0.0.1:5432/goodquestion_ts"
```

로컬 도커 컨테이너 `gq-pg` 가 이미 떠 있다. ⛔ `drizzle-kit push` 는 어떤 이유로도 치지 마라.

⚠️ `.env.local` 에는 **진짜 Gemini API 키**가 들어 있다. 검사는 LLM 을 부르지 않는다
(가짜 응답을 넣어 돌린다). **실제 호출로 확인하고 싶으면 돈이 나간다는 것을 알고 해라.**

## ④ 만들 것 (1) — 프롬프트 두 폴더

```
prompts/report_analysis/   보낼것.md (영어, 태그 [A-…]) · 해설.md (한국어)
prompts/report_guide/      보낼것.md (영어, 태그 [G-…]) · 해설.md (한국어)
```

### 이 레포의 프롬프트 규율 (`prompts/README.md` 가 정본)

- **폴더 하나 = 프롬프트 하나, 파일 둘.** `보낼것.md` 는 **오리거나 다듬지 않고 통째로**
  system 으로 나간다. `해설.md` 는 **한 글자도 안 나간다** — 왜 이렇게 정했나 · 재료 열쇠 표 ·
  검수 이력이 여기 산다.
- ⛔ **자리표시자를 쓰지 마라.** `{child_name}` 같은 글자는 **안 채워지고 그대로 모델에게
  나간다.** user 본문은 언제나 `materialJson(재료)` **JSON 한 덩이**다.
- **태그**는 규칙 한 덩이에 하나씩 붙이고 **두 파일에 같은 태그**를 쓴다. 검사가 한쪽에만
  있는 태그를 이름까지 찍어 잡는다.
- **나가는 쪽은 영어**다 (토큰 절약). 단 **판정 예시 발화는 한국어**로 둔다 — 대상이
  한국어 발화라 번역하면 예시가 아니게 된다.
- ⛔ `보낼것.md` 에 `docs/` · `CLAUDE.md` · `notes/` · `이슈 #` · 📄 · ✏️ 를 쓰지 마라.
  **인용 누출 검사가 잡는다.**

### 내용에서 지켜야 할 것 (명세 5.5)

- **읽는 사람은 부모다.** 아이에게 하는 말투가 아니다.
- ⛔ **평가·등수·진단 금지.** "또래보다" · "부족합니다" · "지연" 같은 말을 쓰지 않는다.
- **보완할 점은 반드시 다음 행동으로.** 「~하면 좋아요」 꼴로 끝낸다.
- 이야기 줄거리를 바꾸거나 없는 장면을 지어내지 않는다.
- 🔴 **「표현」 카드의 근거는 셋뿐이다** — 인물 말을 그대로 흉내 낸 **직접 인용** ·
  **의성어·의태어** · **꾸밈말**. **음성을 저장하지 않으므로** 목소리·톤·크기를 말하는
  문장은 금지한다 (결정 R22).
- 아이 이름은 프롬프트에 **넣되 로그에 남기지 않는다** (결정 R12).

출력 JSON 규격은 명세 5.2(`report_analysis`) · 5.4(`report_guide`) 그대로다.
`report_guide` 의 두 배열은 **각각 3개 고정**이고 `element` 는 8요소 코드여야 한다.

## ⑤ 만들 것 (2) — 엔진 `src/report/engine/`

- **재료 조립** — `metrics` 전부 + `quotes[]` + 아이 이름·나이 + 이야기·장면 이름을
  한 덩이로 묶는다. 본보기는 `src/llm/engine/material.ts`.
- **두 프롬프트를 동시에 부른다** — 서로를 안 보므로 지연은 둘 중 느린 쪽이다.
- 🔴 **인용 id 대조.** LLM 이 낸 `quote_message_id` 가 재료로 준 `quotes[]` 안에 없으면
  **그 인용을 버리고 카드는 인용 없이 남긴다**(`quote_message_id: null`). 이게 이 갈래의
  핵심이다 — LLM 이 **없는 발화를 지어내 따옴표 안에 넣는 것**을 막는 자리다.
- 🔴 **낱말 대조.** 뽑힌 낱말이 실제 아이 발화 안에 글자로 없으면 버린다 (결정 R21).
  `src/llm/judge.ts` 의 `skeleton()` 이 같은 일(근거가 대사 안에 있나)을 이미 한다 —
  **그 함수를 보고 같은 결로** 처리해라.
- 한쪽 LLM 이 실패해도 **다른 쪽 결과는 살린다.** 둘 다 실패하면 부르는 쪽(#38)이
  `status='metrics_only'` 로 저장한다 — **저장은 이 갈래가 하지 않는다.**

⚠️ **낱말을 LLM 이 뽑는 것은 결정 R5(「근거는 규칙이 뽑는다」)의 유일한 예외**다.
한국어를 낱말로 쪼개려면 형태소 분석기가 필요한데 넣지 않기로 했다 (명세 5.3).
LLM 은 **뽑기만** 하고 「새 낱말인가」 판정은 규칙(#36)이 한다.

## ⑥ 🔴 먼저 빨개지는 검사 — `tests/prompts.test.ts`

폴더를 둘 늘리는 순간 **이 검사가 먼저 깨진다.** 미리 알고 같이 고쳐라.

| 어디 | 지금 값 | 왜 깨지나 |
|---|---|---|
| `tests/prompts.test.ts:45` `재료를_받는_것` | 다섯 | 새 폴더 둘을 **여기 더해야** 태그 짝 검사가 돈다 |
| `:71` `'정본 여덟이 다 읽힌다'` | 여덟 | 폴더가 **열**이 된다. 검사 이름도 같이 고쳐라 |
| `:117~122` **47쌍 고정 단언** | `expect(합).toBe(47)` | 네 태그 수만큼 는다. **센 값을 주석에 적어 두어라** |
| `prompts/README.md` 0절·3절·8절 | 여덟 폴더 · 47쌍 | 규율 문서다. **같이 고친다** |

⛔ **`/prompt-lab`(프롬프트 작업대)의 실험 대상을 늘리지 마라.** 지금 `analysis` ·
`character` 둘로 **세 곳**(`src/llm/repo/prompt-lab.ts:24` · `schema.ts:728` 의 DB CHECK ·
`src/llm/service/prompt-lab.ts:102`)에 못박혀 있고, 얹으려면 셋을 다 고쳐야 한다.
**이번 범위 밖이다** (명세 5.6).

## ⑦ 끝났다고 말할 수 있는 조건

- [ ] 두 폴더에 `보낼것.md`/`해설.md` 가 다 있고 **태그 짝이 맞는다**.
- [ ] `보낼것.md` 에 `docs/` · `CLAUDE.md` · `이슈 #` 같은 인용 누출이 없다.
- [ ] `보낼것.md` 에 자리표시자(`{…}`)가 없다.
- [ ] **목록 밖 `quote_message_id`** 를 준 가짜 응답에서 그 인용이 버려진다 (검사).
- [ ] **발화에 없는 낱말**을 낸 가짜 응답에서 그 낱말이 버려진다 (검사).
- [ ] `tests/prompts.test.ts` 의 폴더 수·태그 쌍 단언이 새 값으로 통과한다.
- [ ] `pnpm typecheck` · `pnpm lint` 통과.
- [ ] `pnpm test` — **이 변경 전과 실패 개수가 같거나 줄었다.** 늘었으면 네가 깬 것이다.

검사 자리는 `tests/report-narrative.test.ts` 다 (명세 11절).

## ⑧ 남의 갈래를 건드리지 마라

지금 **세 갈래가 동시에 돈다.** 파일이 겹치면 합칠 때 사람이 손으로 푼다.

| 갈래 | 만지는 곳 |
|---|---|
| P-1 (#35) | `sql/006_*.sql` · `src/llm/db/schema.ts` · `src/llm/db/seed.ts` |
| P-2 (#36) | `src/report/domain/` · `tests/report-metrics.test.ts` |
| **P-3 (너, #37)** | `prompts/report_*/` · `src/report/engine/` · `tests/prompts.test.ts` · `tests/report-narrative.test.ts` · `prompts/README.md` |

- ⛔ **`src/report/types.ts` 를 고치지 마라.** 세 갈래가 공유하는 계약이고 프론트 계약
  문서 1절을 그대로 옮긴 것이다. 모자란 게 있으면 **고치지 말고 마지막 답변에 적어라.**
- ⛔ **`src/report/domain/metrics.ts` 를 만들지 마라.** P-2 의 몫이다. 네 엔진은
  `ReportMetrics` 를 **인자로 받는다** — 그 타입은 `src/report/types.ts` 에 이미 있다.
- ⛔ **DB 에 저장하지 마라.** `parent_reports` 표는 P-1 이 세우고 저장은 P-4(#38)가 한다.
- ⛔ **`src/llm/db/schema.ts` 를 고치지 마라.**

## ⑨ 다 하면

```bash
git add -A
git commit    # 아래 형식
git push -u origin rlaqudwn1/issue-37-report-prompts
```

커밋 메시지는 이 레포의 결을 따른다 — **첫 줄은 한국어 한 문장, 끝에 ` — #37`**.
본문에는 「무엇을 왜」와 「확인한 것」을 적는다. 최근 커밋(`git log -3`)을 본보기로 봐라.

트레일러 두 줄을 끝에 붙인다:

```
Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01FKq5C6XMcXiiNAqikbRdfi
```

⛔ **PR 을 열지 마라. 머지하지 마라.** 푸시까지만 하고, 무엇을 했고 무엇이 남았는지
마지막 답변에 적어라. 본선 세션이 세 갈래를 모아 합친다.

## ⑩ 막히면

- 판단이 갈리는 자리가 나오면 **명세를 이긴다고 생각하지 말고** 명세대로 하고,
  이상하다고 느낀 점을 마지막 답변에 적어라.
- 명세에 없는 것을 정해야 하면 **가장 되돌리기 쉬운 쪽**을 고르고 그 사실을 적어라.
- 이 레포의 코드는 **한국어 식별자와 한국어 주석**을 쓴다. 주변 코드의 결을 따라라.
