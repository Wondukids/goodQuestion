# 갈래 후-3ᵇ — 단어 판정 엔진 (이슈 #44 의 뒤쪽 절반)

> 이 워크트리는 **혼자 도는 갈래**다. 앞 대화도 프로젝트 메모리도 없다.
> 이 문서 하나로 시작할 수 있게 썼다. **먼저 끝까지 읽어라.**

## ⓪ 여기가 무슨 프로젝트인가

6~9세 아이가 옛이야기(「방귀 뀌는 며느리」) 속 캐릭터와 **말로 대화하는** 앱이다.
이야기를 다 보고 나면 활동이 하나 더 있다 — **말하기 후 활동**. 장면 카드 넉 장을
순서대로 놓고, 그 카드와 **핵심 단어 12개**를 보면서 줄거리를 통째로 들려주는 활동이다.

아이가 말한 줄거리를 서버가 받아쓴 다음, **그 12개 단어를 아이가 썼는지**를 가려 기록한다.
보호자 리포트에 「이 단어를 같이 써 보세요」로 보이는 값이다.

## ① 네가 할 일 — 한 줄

**받아쓴 줄거리 한 덩이를 받아 단어 12개의 판정을 내는 함수를 짠다.**

```
받는 것: 아이가 말한 줄거리 원문 + 카드 넉 장(제목·단어 셋씩) + 이야기 제목·요지
내는 것: 단어 12개 × { card_id, word, status, evidence, decided_by }
                       status: 'used' | 'similar' | 'missing'
                       decided_by: 'rule' | 'llm'
```

## ② 판정은 3단이다 — 이 그림이 네 일의 전부다 (명세 6.1)

```
①  규칙이 글자로 찾는다   → 찾히면  status='used',    decided_by='rule'   (LLM 안 부른다)
       │ 못 찾은 것만
       ▼
②  LLM 에게 묻는다        → 참이면  status='similar', decided_by='llm'
       │  「비슷한 말로 말했나」 하나만
       ▼
③  근거를 원문과 대조한다 → 원문에 없으면 그 판정을 버리고 status='missing'
```

**활동 한 건에 LLM 호출은 한 번뿐이다** (명세 6.3). 단어마다 부르지 마라.

## ③ 🟢 이미 다 있는 것 — 새로 만들지 마라

| 있는 것 | 어디 | 무엇 |
|---|---|---|
| **프롬프트 두 파일** | `prompts/retelling_keywords/` | ⭕ **방금 다른 갈래가 끝냈다.** 새로 쓰지 마라 |
| 표 둘 | `sql/007_post_activity.sql` · `src/llm/db/schema.ts` | `post_activity_results` · `post_activity_keywords` — ⭕ **이미 섰다** |
| 카드·단어 값 | DB `stories.post_activity_config` | 카드 넉 장·정답 순서·섞인 순서 |
| **글자 대조기** | `src/report/engine/verify.ts` | `낱말이_발화에_있나()` · `낱말_후보들()` · `발화_뼈대()` · `최소_어간_길이` — 전부 `@/report/engine` 에서 내보낸다 |
| **LLM 부르는 본보기** | `src/report/engine/narrative.ts` | `chooseBody(폴더이름)` → `complete(본문, 재료, …)`. 이 결을 그대로 따라라 |
| 층 규칙 | `src/report/README.md` | 순수 함수 / 엔진 / 서비스 / 리포를 어떻게 가르나 |

## ④ 시작하기 전에 읽을 것

| 순서 | 파일 | 무엇 |
|---|---|---|
| 1 | `docs/말하기후활동_명세.md` **6절** | 🔴 정본. 3단 · 재료 · 출력 · 두 글자 바닥 |
| 2 | `prompts/retelling_keywords/보낼것.md` | **네가 부를 지시문.** 규칙 열 개가 무엇을 약속하는지 |
| 3 | 같은 폴더 `해설.md` | 왜 그렇게 썼나 · 재료 열쇠 표 |
| 4 | `src/report/engine/narrative.ts` 앞부분 | LLM 을 부르고 응답을 검증하는 결 |
| 5 | `src/report/engine/verify.ts` 85~125행 | 글자 대조가 실제로 어떻게 도나 |

이슈 원문: `gh issue view 44` (그중 **엔진 쪽만** 네 몫이다 — 프롬프트는 끝났다)

## ⑤ 🔴 프롬프트와의 계약 — 어긋나면 조용히 틀린다

앞 갈래가 쓴 `보낼것.md` 는 **이렇게 약속돼 있다.** 네 코드가 이 약속을 받아야 한다.

- **`similar` 하나만 낸다.** `{"words":[{"word","similar","evidence"}]}` 뿐이다.
  `used`·`missing` 으로 가르는 것은 **네 코드**다.
- **안 쓴 단어의 `evidence` 는 빈 문자열(`""`)로 온다.** DB 의 `evidence` 는 **NULL 이
  정본**이다 (명세 4.3 — `missing` 이면 인용할 말이 없다). 🔴 **빈 문자열을 NULL 로 옮겨라.**
- **재료(user)에는 「판정할 단어 목록」만 넣는다** — ①이 거르고 남은 것. 열쇠는 `story` ·
  `cards` · `words` · `retelling` 넷이다 (`보낼것.md` 의 「받는 것」 표가 정본).
- **12줄 경계 표는 프롬프트 안에 산다.** ⛔ **그 판단을 코드로 옮기지 마라.** 판정이
  흔들리면 표를 고치지 엔진을 고치지 않는다.

## ⑥ 🔴 두 글자 바닥 (F14) — 이 갈래에서 가장 사고 나기 쉬운 자리

`낱말이_발화에_있나()` 는 「~다」로 끝나는 낱말의 **어간을 떼어** 찾는다. 그런데
**두 글자 낱말은 어간이 한 글자**가 된다 — 「참다」 → 「참」.

한 글자는 **「참새를 봤어요」·「참 재밌었어요」에도 걸린다.** 그대로 쓰면 아이가 방귀를
참은 이야기를 한 마디도 안 했는데 `used` 가 찍힌다.

> 🔴 **후활동 판정에서는 두 글자 미만 조각을 쓰지 않는다.** 그렇게 아무 조각도 안 남는
> 낱말은 ①을 건너뛰고 곧장 ②(LLM)로 보낸다.
>
> ⛔ **`verify.ts` 를 고치지 마라.** 그 함수는 **보호자 리포트의 낱말 세기**가 쓰고 있고
> 거기서는 지금 동작이 맞다(성기게 잡아도 되는 자리다). 고치면 리포트 숫자가 함께 바뀐다.
> **부르는 쪽(네 코드)에서 바닥을 걸어라.** `낱말_후보들()` 이 이미 내보내져 있으니 그것을
> 받아 두 글자 미만을 걸러 쓰면 된다. `최소_어간_길이` 도 함께 내보내져 있으니 먼저 읽어라.

수용 기준이 이 한 줄을 그대로 잰다 — **「참다」를 「참새를 봤어요」로만 말하면 `used` 가
되지 않는다** (명세 9절 7번).

## ⑦ 🔴 그 밖에 놓치면 사고 나는 것

### ① LLM 판정 실패는 오류가 아니다 (F4·F8)

받아쓴 글은 **판정보다 먼저 저장**되고, 판정이 실패하면 `analyzed: false` 로 **200** 이
나간다. `analyzed_at` 은 NULL 로 남는다. **네 함수는 던지지 말고 「판정 못 했다」를
돌려줘야 한다.** `generateReport()`(`src/report/service/generate.ts`)가 같은 이유로 안
던진다 — 그 결을 봐라.

「판정을 못 했다」와 「단어를 하나도 안 썼다」는 **다른 말**이다. 뒤엣것은 12행이 전부
`missing` 이고 `analyzed_at` 이 차 있다.

### ② 근거 대조는 네가 한다 (③단)

`evidence` 가 아이 원문에 **실제로 없으면 그 판정을 버리고 `missing` 으로 떨어뜨린다.**
리포트의 인용 검증(`verify.ts`)과 같은 결이다. 지어낸 근거가 보호자 화면에 「아이가 이렇게
말했어요」로 뜨는 것을 막는 자리다.

### ③ 판정한 단어는 12개 **전부** 행이 된다

`missing` 도 남는다. 「없는 것」을 세려면 행이 있어야 한다.
다시 판정하면 그 `result_id` 의 행을 **모두 지우고 다시 넣는다** (명세 4.3).

### ④ STT 는 한 번에 55초까지다

아이가 길게 말하면 잘리고, 잘린 뒤에 있던 단어는 `missing` 이 된다. 고칠 것은 아니고
**알고 있어야 하는 것**이다.

## ⑧ 어디에 두나 — 이 킥오프가 정한다 (명세에 없다)

`src/post-activity/` 를 새로 판다. 층 이름은 `src/report/README.md` 의 결을 따른다
(순수 함수는 `domain/`, LLM 을 부르는 것은 `engine/`).

- 후활동은 **리포트도 대화도 아니다.** `src/report/` 아래 두면 리포트 층 규칙에 얹히고,
  `src/llm/engine/` 에 두면 대화 턴 엔진과 섞인다.
- 뒤 갈래(#45)가 컨트롤러를 `src/session/controller/post-activity.ts` 에 둔다 (명세 5절).
  그 컨트롤러가 네 함수를 부른다 — **부르기 쉬운 하나의 진입점**을 내보내라.
- 🟡 되돌려도 되는 판단이다. 다르게 두었으면 **커밋 본문에 적어라.**

⛔ **DB 에 쓰는 것은 네 일이 아니다.** 네 함수는 **판정 결과를 돌려줄 뿐**이고, 저장은
#45 가 한다. 리포 층(`src/*/repo/`)을 만들지 마라.

## ⑨ 손대지 마라

- ⛔ **`prompts/**`** — 앞 갈래가 끝냈다. 프롬프트 글을 고치고 싶어지면 **고치지 말고
   마지막 답변에 적어라.** 고치면 검사(태그 짝 90쌍)가 함께 흔들린다.
- ⛔ **`src/report/engine/verify.ts`** (⑥의 이유) · `src/report/types.ts`(공유 계약).
- ⛔ **리포트 시점 이관** (`src/report/service/generate.ts` · `src/llm/service/run.ts` ·
   `story.ts`) — **지금 다른 워크트리가 그 갈래(#43)를 짜고 있다.** 열어 보더라도 읽기만.
- ⛔ **API·라우트** (명세 5절) — #45 것이다.
- ⛔ **미션 1·2** (`mission_*`) · `sql/**` · `src/llm/db/schema.ts` — 표는 다 서 있다.
- ⛔ **`finale.tsx` · `finale-script.ts`** — 화면은 #46 것이다.

## ⑩ 다 하면

- [ ] 명세 9절의 **수용 기준 6·7·8·9** 를 **단위 검사로** 남긴다:
      6. 「시집」을 「시집와서」로 말하면 `used` · `rule` (LLM 호출 없이)
      7. 「참다」를 「참새를 봤어요」로만 말하면 **`used` 가 되지 않는다** (두 글자 바닥)
      8. 「참다」를 「꾹 눌렀어요」로 말하면 `similar` · `llm` · `evidence` 가 원문 안에 있다
      9. LLM 이 원문에 없는 `evidence` 를 내면 그 단어는 `missing`
      ⚠️ **LLM 은 가짜로 갈아 끼워 검사한다.** `narrative.ts` 가 `options.call` 로 그 자리를
      열어 둔 것을 본보기로 봐라 — 검사에서 진짜 API 를 치지 마라(돈이 나가고 흔들린다).
- [ ] LLM 이 죽었을 때 **던지지 않고** 「판정 못 했다」가 나오는 것도 검사로 남겨라 (수용 기준 10).
- [ ] `pnpm typecheck` 통과 · `pnpm lint` 는 변경 전과 같은지만 본다.
- [ ] `pnpm test` — **기준선 대비 실패가 늘지 않았다.** 🔴 시작하자마자 아무것도 고치기 전에
      한 번 돌려 기준선을 재 둬라. DB 검사가 5초 제한에서 흔들려 회차마다 숫자가 크게 다르다.

```bash
git add -A
git commit    # 첫 줄은 한국어 한 문장, 끝에 ` — #44`
git push -u origin rlaqudwn1/issue-44-retelling-engine
```

본문에 「무엇을 왜」와 「확인한 것」. `git log -3` 을 본보기로 봐라. 끝에 트레일러:

```
Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
```

⛔ **PR 을 열지 마라. 머지하지 마라.** 푸시까지만 하고 마지막 답변에 상황을 적어라.

## ⑪ 환경 세팅

```bash
pnpm install                                                    # node_modules 가 없다
cp "C:/Users/rlaqu/Documents/GitHub/goodQuestion-wondukids/.env.local" .env.local
npx next typegen                       # .next/types 가 없어 typecheck 가 7개 헛터진다
```

🔴 검사를 돌릴 때는 `DATABASE_URL` 을 **로컬로** 바꿔라. 본선 값은 팀의 라이브 Supabase 라
레포 가드가 DB 검사를 막는다.

```
DATABASE_URL="postgresql://postgres:gqlocal@127.0.0.1:5432/goodquestion_ts"
```

로컬 도커 `gq-pg` 에 **007 까지 먹여져 있다.** ⛔ `drizzle-kit push` 금지. ⛔ 팀 DB 에 쓰지 마라.

## ⑫ 막히면

- 판단이 갈리면 **명세를 이긴다고 생각하지 말고** 명세대로 하고, 이상한 점을 적어라.
- 명세에 없는 것을 정해야 하면 **가장 되돌리기 쉬운 쪽**을 고르고 그 사실을 적어라.
- 엔진 쪽 코드(`src/llm` · `src/report` · `src/session`)는 **한국어 식별자와 한국어 주석**을
  쓴다. 주변 코드의 결을 따라라.
